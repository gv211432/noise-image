{
  "name": "realistic-camera-noise",
  "version": "1.0.0",
  "description": "Add realistic camera sensor noise to AI-generated images",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "build": "tsc",
    "type-check": "tsc --noEmit"
  },
  "keywords": [
    "image-processing",
    "camera-noise",
    "sensor-noise",
    "sharp",
    "typescript"
  ],
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/bun": "^1.1.15",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2"
  }
}
